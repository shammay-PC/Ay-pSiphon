# 🚀 Ay-pSiphon: نصب آسان Psiphon Tunnel Core برای لینوکس

این یک مخزن غیررسمی است که هدف آن تسهیل فرآیند نصب ابزار رایگان Psiphon VPN (هسته تونل) برای دور زدن فیلترینگ بر روی سیستم‌عامل لینوکس است.

Psiphon یک ابزار قوی برای دسترسی به محتوای مسدود شده و حفظ حریم خصوصی در هنگام مرور اینترنت است.

---

## 🌟 ویژگی‌ها

* **نصب ساده:** تمامی فایل‌های ضروری را دانلود و در مسیرهای صحیح نصب می‌کند.
* **اجرای خودکار:** با استفاده از سرویس `systemd`، Psiphon پس از نصب شروع به کار کرده و با هر بار راه‌اندازی سرور به صورت خودکار اجرا می‌شود.
* **پیکربندی پیش‌فرض:** شامل یک فایل پیکربندی پیش‌فرض (`pSiphon.config`) با پورت‌های استاندارد است.
* **اسکریپت حذف نصب:** دارای یک اسکریپت ساده برای حذف سریع تمامی فایل‌های نصب شده.

---

## 💻 نصب و راه‌اندازی

برای نصب `Ay-pSiphon`، کافی است اسکریپت نصب را با دسترسی `root` اجرا کنید.

1.  **دانلود اسکریپت نصب:**

    ```bash
    wget https://raw.githubusercontent.com/shammay-PC/Ay-pSiphon/main/Ay-pSiphon
    ```

2.  **اجرای اسکریپت با دسترسی Root (اجرای نصب):**

    ```bash
    sudo sh Ay-pSiphon
    ```

3.  **پس از اتمام نصب:**

    نصب با پیام "Ay-pSiphon for Linux installation successfully completed." به پایان می‌رسد.
    سرویس `systemd` به صورت خودکار فعال و راه‌اندازی می‌شود.

---

## 🛠️ مدیریت سرویس (Systemd)

پس از نصب، می‌توانید وضعیت سرویس `Ay-pSiphon` را با دستورات زیر مدیریت کنید:

* **بررسی وضعیت:**

    ```bash
    sudo systemctl status ay-psiphon.service
    ```

* **توقف سرویس:**

    ```bash
    sudo systemctl stop ay-psiphon.service
    ```

* **راه‌اندازی مجدد سرویس:**

    ```bash
    sudo systemctl restart ay-psiphon.service
    ```

* **غیرفعال‌سازی اجرای خودکار در بوت:**

    ```bash
    sudo systemctl disable ay-psiphon.service
    ```

---

## 🛑 حذف نصب

اسکریپت حذف نصب (`UnAy-pSiphon`) تمامی فایل‌هایی که در حین نصب ایجاد شده‌اند را حذف می‌کند.

**توجه:** اسکریپت حذف نصب فقط در صورتی کار می‌کند که آن را از پوشه‌ای اجرا کنید که فایل `UnAy-pSiphon` در آن قرار دارد.

```bash
# مطمئن شوید در پوشه‌ای هستی که UnAy-pSiphon در آن دانلود شده است
sudo sh UnAy-pSiphon
```

این دستور فایل‌های زیر را حذف می‌کند:

* `/etc/Ay-pSiphon`
* `/usr/bin/psiphon`
* `Ay-pSiphon`
* `ca.psiphon.PsiphonTunnel.tunnel-core`
* `UnAy-pSiphoner`

---

## 🔑 اطلاعات پیکربندی

فایل پیکربندی (`/etc/Ay-pSiphon/pSiphon.config`) پورت‌های زیر را برای پروکسی‌های محلی تنظیم می‌کند:

* **پروکسی HTTP محلی:** پورت `8081`
* **پروکسی SOCKS محلی:** پورت `1081`

شما می‌توانید با ویرایش این فایل، تنظیمات منطقه‌ای (`EgressRegion`) یا پورت‌ها را تغییر دهید.

---

## 🤝 مشارکت و مجوز

این پروژه غیررسمی است و برای آسان‌سازی استفاده از Psiphon برای کاربران لینوکس ایجاد شده است.

هسته اصلی تونل Psiphon توسط Psiphon-Labs توسعه یافته است.

---

**مخازن مرتبط:**

* [Psiphon Tunnel Core Binaries](https://github.com/Psiphon-Labs/psiphon-tunnel-core-binaries)
* [Psiphon-Labs](https://github.com/Psiphon-Labs)